logging:
  level:
    org.springframework.cloud: 'DEBUG'
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lizwmaster/spring-cloud-config
          search-paths:
            - spring-cloud-configs
