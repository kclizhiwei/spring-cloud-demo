spring:
  cloud:
    config:
      name: config-server
      profile: dev,redis
      label: master
      uri: http://localhost:8888
  application:
    name: config-client
  config:
    name: config-server
